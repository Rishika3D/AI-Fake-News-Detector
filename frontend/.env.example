# ── API ───────────────────────────────────────────────────────────────────────
# Points the frontend at the backend.
# Local dev: leave blank — defaults to http://localhost:5050/api/analyze
# Production: set to your deployed backend URL
VITE_API_URL=https://your-backend-domain.onrender.com/api/analyze
